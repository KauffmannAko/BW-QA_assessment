{
  "name": "bw-qa_assessment",
  "version": "1.0.0",
  "description": "BW QA assessment",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cy:open": "cypress open",
    "cy:headless" :"cypress run --headless",
    "cy:html":"cypress run --reporter cypress-html-reporter"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^12.17.2"
  }
}
